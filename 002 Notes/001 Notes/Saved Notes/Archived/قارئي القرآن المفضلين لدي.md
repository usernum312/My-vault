---
aliases:
  - My favorite Quran Readers
link pages:
  - "[[Quran|المصحف]]"
Categories:
  - "[[Diny|ديني]]"
  - "[[Mawaidh|مواعظ]]"
icon: lucide-users
---
```dataviewjs
const SURAH_NAMES = [
  "الفاتحة", "البقرة", "آل عمران", "النساء", "المائدة", "الأنعام", "الأعراف", "الأنفال", 
  "التوبة", "يونس", "هود", "يوسف", "الرعد", "إبراهيم", "الحجر", "النحل", "الإسراء", 
  "الكهف", "مريم", "طه", "الأنبياء", "الحج", "المؤمنون", "النور", "الفرقان", "الشعراء", 
  "النمل", "القصص", "العنكبوت", "الروم", "لقمان", "السجدة", "الأحزاب", "سبأ", "فاطر", 
  "يس", "الصافات", "ص", "الزمر", "غافر", "فصلت", "الشورى", "الزخرف", "الدخان", 
  "الجاثية", "الأحقاف", "محمد", "الفتح", "الحجرات", "ق", "الذاريات", "الطور", "النجم", 
  "القمر", "الرحمن", "الواقعة", "الحديد", "المجادلة", "الحشر", "الممتحنة", "الصف", 
  "الجمعة", "المنافقون", "التغابن", "الطلاق", "التحريم", "الملك", "القلم", "الحاقة", 
  "المعارج", "نوح", "الجن", "المزمل", "المدثر", "القيامة", "الإنسان", "المرسلات", 
  "النبأ", "النازعات", "عبس", "التكوير", "الانفطار", "المطففين", "الانشقاق", "البروج", 
  "الطارق", "الأعلى", "الغاشية", "الفجر", "البلد", "الشمس", "الليل", "الضحى", "الشرح", 
  "التين", "العلق", "القدر", "البينة", "الزلزلة", "العاديات", "القارعة", "التكاثر", 
  "العصر", "الهمزة", "الفيل", "قريش", "الماعون", "الكوثر", "الكافرون", "النصر", 
  "المسد", "الإخلاص", "الفلق", "الناس"
];

const COPY_ICON_SVG = `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>`;
const CHECK_ICON_SVG = `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>`;

const activeFile = app.workspace.getActiveFile();

if (activeFile) {
    const content = await app.vault.read(activeFile);
    
    let contentWithoutFrontmatter = content;
    const frontmatterMatch = content.match(/^---\n([\s\S]*?)\n---\n/);
    if (frontmatterMatch) {
        contentWithoutFrontmatter = content.replace(frontmatterMatch[0], '');
    }
    
    const cleanContent = contentWithoutFrontmatter.replace(/```[\s\S]*?```/g, '');
    
    const reciters = cleanContent.split('\n')
        .map(line => line.trim())
        .filter(line => line.startsWith('-') || line.startsWith('*'))
        .map(line => {
            let name = line.replace(/^[-*]\s*/, '').trim();
            name = name.replace(/\[\[([^\]|]+)(?:\|[^\]]+)?\]\]/g, '$1');
            return name.trim();
        })
        .filter(name => name.length > 0);

    if (reciters.length === 0) {
        dv.paragraph("لم يتم العثور على قائمة قراء في الصفحة.");
    } else {
        const wrapper = dv.el('div', '');
        Object.assign(wrapper.style, {
            display: 'flex',
            alignItems: 'center',
            gap: '0.75rem',
            margin: '0.5rem 0',
            direction: 'rtl'
        });
        
        const button = dv.el('button', 'اختر قارئ');
        Object.assign(button.style, {
            cursor: 'pointer',
            border: '1px solid var(--background-modifier-border)',
            backgroundColor: 'var(--interactive-accent)',
            borderRadius: '4px',
            fontWeight: 'bold',
            padding: '0.5rem 1rem',
            whiteSpace: 'nowrap',
            height: '2.65rem',
            color: 'var(--text-on-accent, var(--text-normal))'
        });
        
        const outputContainer = dv.el('div', '');
        Object.assign(outputContainer.style, {
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            flex: '1',
            padding: '0.5rem 0.75rem',
            backgroundColor: 'var(--background-secondary)',
            borderRadius: '4px',
            border: '1px solid var(--background-modifier-border)',
            height: '2.65rem',
            fontSize: '0.95rem',
            color: 'var(--text-normal)',
            overflow: 'hidden'
        });

        const textSpan = dv.el('span', '');
        textSpan.style.overflow = 'hidden';
        textSpan.style.textOverflow = 'ellipsis';
        textSpan.style.whiteSpace = 'nowrap';

        const copyIconBtn = dv.el('span', '');
        copyIconBtn.innerHTML = COPY_ICON_SVG;
        Object.assign(copyIconBtn.style, {
            cursor: 'pointer',
            display: 'inline-flex',
            alignItems: 'center',
            justifyContent: 'center',
            padding: '6px',
            borderRadius: '4px',
            color: 'var(--text-muted)',
            transition: 'color 0.2s, background-color 0.2s',
            marginLeft: '0.25rem'
        });

        copyIconBtn.addEventListener('mouseenter', () => {
            copyIconBtn.style.color = 'var(--text-normal)';
            copyIconBtn.style.backgroundColor = 'var(--background-modifier-hover)';
        });
        copyIconBtn.addEventListener('mouseleave', () => {
            copyIconBtn.style.color = 'var(--text-muted)';
            copyIconBtn.style.backgroundColor = 'transparent';
        });

        outputContainer.appendChild(textSpan);
        outputContainer.appendChild(copyIconBtn);
        
        wrapper.appendChild(button);
        wrapper.appendChild(outputContainer);
        
        const selectRandom = () => {
            const randomSurah = SURAH_NAMES[Math.floor(Math.random() * SURAH_NAMES.length)];
            const randomReciter = reciters[Math.floor(Math.random() * reciters.length)];
            textSpan.textContent = `سورة ${randomSurah} بصوت القارئ: ${randomReciter}`;
        };

        selectRandom();
        button.addEventListener('click', selectRandom);

        copyIconBtn.addEventListener('click', async () => {
            try {
                await navigator.clipboard.writeText(textSpan.textContent);
                
                copyIconBtn.innerHTML = CHECK_ICON_SVG;
                copyIconBtn.style.color = 'var(--text-success)';
                
                setTimeout(() => {
                    copyIconBtn.innerHTML = COPY_ICON_SVG;
                    copyIconBtn.style.color = 'var(--text-muted)';
                }, 1500);
            } catch (err) {
                console.error('فشل النسخ: ', err);
            }
        });
    }
} else {
    dv.paragraph("تعذر تحديد الملف الحالي.");
}
```
-  خالد كريم محمدي
- سيد أحمد هاشمي
- عبدالودود حنيف
- يوسف الدغوش
- محمد اللحيدان
- عبدالله البريمي
- ناصر العصفور
- ناصر القطامي
- هزاع البلوشي
- عادل الكلباني
- سعود الشريم
- عبدالله فهمي
- هيثم الدخين
- صلاح البدير
- محمد الفقيه
- محمد البراك
- محمد خليل
- فهد العتيبي
- هشام الهزاز
- أشرف إمام
- بدر التركي