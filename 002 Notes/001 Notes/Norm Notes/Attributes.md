Add that's three optional attributes on the **action** reward (actionTaskCosts)
pkg: string: if there's there specific app launched,
cost_time: integer: Estimated time to complete the action (At the bottom of the page, a timer is created based on cost), default 5min
effective: Boolean: if we should cost it from allowed time (If the cost needs to be calculated and the value adjusted (the value of the .timer.md file and the available time)) default: false

```dataviewjs

```