---
Categories:
  - "[[Tasks|Task]]"
---
My CSS writing isn't very good, so I'd appreciate it if you could fix it. Often, I separate the selectors even though I could group them all inside `is` and other code. The code should work as it is now, but with readable and cleaned code.
Restriction: Use of `is`in the root is prohibited It is permissible to use it within a specified area to avoid duplication, but not outside the specified area, and to make it the first layer.