---
Categories:
  - "[[Tasks]]"
---
I will add two feature in the AI assistant plugin 
one user can allow to AI change the pages and files content directly The goal of this feature isn't just to add something meaningless, but rather to emulate AI-powered file editors like Cursor. It should be able to edit the existing code within files directly. For instance, there could be an option where the user attaches a folder and tells the AI: 'Edit the files in this folder and improve the context.' The AI would then modify the text inside those notes. However, before applying any changes, a window would pop up asking the user, 'Do you approve of these changes?' and showing them the modifications the AI prepared—highlighting what will be replaced or deleted, just like the diff view on GitHub when making a commit."
two user can select folder as attachment (not just file)