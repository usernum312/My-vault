---
Categories:
  - "[[Tasks|Task]]"
---
I'm thinking about making the reminder window for adding Islamic times look like the system window style in anime, like in the picture and others. What I'm thinking is having an option that the user can turn on or off called `solo up reminder`. Then the notification would look like this:  
- At the top of the window on the right side (or the left side depending on the language direction) there’s the word `مهمة` or `task`  
- Below it there’s a decorative `hr` line in purple  
- Then below it is the task content for which the reminder is set  
- The word `done` or `تم` is replaced with `قبول` or `accept`  
- The word postpone is replaced with `قبول الفشل` or `Accepting failure`  
- The word mute is replaced with `اقتل ضميرك` or `kill your conscience`  
- The background is completely dark black `#000000`  
- The accept button background is `blue`  
- The failure button background is bright red  
- The kill conscience button background is dark deep red  
- The window corners are sharp