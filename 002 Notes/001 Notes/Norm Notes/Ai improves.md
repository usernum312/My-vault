- Sometimes, due to certain circumstances, the differences window may close by mistake, so please add a way to view it again by clicking a button or something similar...
- By the way, I think the AI search feature within files could be improved. I believe the AI could use user input to identify keywords, which would then be sent to the application, allowing it to search all files (in the allowed scope) containing those keywords, After that, all files containing the keywords are sent to the AI, and the AI analyzes them one by one. It then determines the similarity percentage with the file the user asked to modify, while deleting everything except the file name and the similarity percentage from the AI’s memory so that it is not overloaded. In this way, it moves from one file to another. In the end, it searches and compares the files that have the highest matching percentage with the user’s goal. Once it identifies one, it deletes the rest from its memory so it will not be overloaded as well, and then it executes the user’s request.